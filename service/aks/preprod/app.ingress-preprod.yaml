---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: integration-preprod
  namespace: gcd-preprod
  labels:
    website: integration-preprod
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-methods: "PUT, GET, POST, DELETE, OPTIONS"
    nginx.ingress.kubernetes.io/cors-allow-credentials: "true"

spec:
  rules:
    - host:  gcd-integration-preprod.maersk-digital.net
      http:
        paths:
        - path: /
          backend:
            serviceName: integration-preprod
            servicePort: 8081
  tls:
  - hosts:
    -  gcd-integration-preprod.maersk-digital.net
    secretName: tls-wildcard-maersk-digital-net
---

